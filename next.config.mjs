/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['images.unsplash.com','unsplash.com'], // Add Unsplash domain to the list
    },
  };
  
  export default nextConfig;
  